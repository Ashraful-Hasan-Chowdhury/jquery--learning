$(document).ready(function() {
    $('#btnHide').click(
        function() {
            $('#testText').hide();
        }
    );
    // There are a lot more events here
    // dblclick, mouseenter,mouseleave
});
